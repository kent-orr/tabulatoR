% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy.R
\name{tabulatorRevertField}
\alias{tabulatorRevertField}
\title{Revert a cell to its previous value}
\usage{
tabulatorRevertField(
  outputId,
  index,
  field,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{outputId}{The output ID of the Tabulator table (string).}

\item{index}{The row index (1-based) of the cell to revert.}

\item{field}{The field name (column name) of the cell to revert.}

\item{session}{The Shiny session object. Defaults to \code{shiny::getDefaultReactiveDomain()}.}
}
\description{
Programmatically reverts a cell in a Tabulator table back to its previous value.
This is useful when validation fails or when you want to undo a user's edit.
}
\details{
This function calls Tabulator's \code{cell.restoreOldValue()} method, which reverts
the cell to the value it had before the most recent edit.
}
\examples{
\dontrun{
# In a Shiny server function:
observeEvent(input$my_table, {
  event <- input$my_table

  if (event$action == "cellEdited" && event$value < 0) {
    # Revert negative values
    tabulatorRevertField("my_table", event$index, event$field)
  }
})
}

}
