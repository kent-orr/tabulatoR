% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{Column}
\alias{Column}
\title{Define a Tabulator Column}
\usage{
Column(
  title,
  field,
  visible = TRUE,
  hozAlign = NULL,
  width = NULL,
  resizable = NULL,
  editable = FALSE,
  editor = NULL,
  editorParams = NULL,
  formatter = NULL,
  formatterParams = NULL,
  cellClick = NULL,
  cellEdited = NULL,
  ...,
  .opts = list()
)
}
\arguments{
\item{title}{The column title to display in the table header.}

\item{field}{The field name in the data corresponding to this column.}

\item{visible}{Logical. Whether the column is visible. Default is \code{TRUE}.}

\item{hozAlign}{Horizontal text alignment for cells (\code{"left"}, \code{"center"}, \code{"right"}).}

\item{width}{A fixed column width (e.g., "150px" or "20\%").}

\item{resizable}{Logical. Whether the user can resize this column.}

\item{editable}{Logical. If \code{TRUE}, the cells are editable. When \code{TRUE}
and \code{editor} is \code{NULL}, Tabulator guesses the editor (\code{editor = TRUE}).}

\item{editor}{Editor type (\code{"input"}, \code{"number"}, etc.) or JS function via
\code{js()}. Supplying this parameter implicitly enables editing.}

\item{editorParams}{A list of parameters passed to the editor.}

\item{formatter}{Formatter name or JS function (use \code{js()}).}

\item{formatterParams}{A list of parameters passed to the formatter.}

\item{cellClick}{JS function triggered when a cell is clicked (use \code{js()}).}

\item{cellEdited}{JS function triggered after a cell is edited (use \code{js()}).}

\item{...}{Additional named Tabulator column options.}

\item{.opts}{A named list of column options that can be reused programmatically.
Values in \code{...} will override matching keys in \code{.opts}.}
}
\value{
A named list representing a single column definition.
}
\description{
This helper function constructs a Tabulator column definition list suitable for use
in \code{renderTabulatoR()}. It provides a concise set of the most useful column configuration
parameters for Shiny applications while allowing full access to Tabulator's extensive
API via \code{...} or \code{.opts}.
}
\details{
For the full list of column options, see the \href{https://tabulator.info/docs/6.3/columns#definition}{Tabulator documentation}.
JavaScript callbacks (such as \code{cellClick} or \code{formatter}) must be wrapped using \code{js()}
from this package to be interpreted as executable functions in the browser.
Supplying \code{editor} implicitly enables editing. If \code{editable = TRUE} and no
\code{editor} is provided, Tabulator attempts to guess the editor (\code{editor = TRUE}).
}
