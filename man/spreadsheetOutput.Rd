% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spreadsheet.R
\name{spreadsheetOutput}
\alias{spreadsheetOutput}
\title{Output element for a Tabulator spreadsheet}
\usage{
spreadsheetOutput(outputId, width = "100\%", height = "400px", ...)
}
\arguments{
\item{outputId}{The output variable to read the spreadsheet from.}

\item{width}{The width of the spreadsheet container (default: "100\%").}

\item{height}{The height of the spreadsheet container (default: "400px").}

\item{...}{Additional arguments passed to \code{tab_source()} for customizing Tabulator version and theme.}
}
\value{
An HTML widget container for the spreadsheet.
}
\description{
Creates an output element in the Shiny UI to display a Tabulator spreadsheet.
This should be paired with \code{renderSpreadsheet()} in the server function.
}
\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    spreadsheetOutput("my_spreadsheet", height = "600px")
  )

  server <- function(input, output, session) {
    output$my_spreadsheet <- renderSpreadsheet(
      data.frame(
        A = 1:10,
        B = 11:20,
        C = 21:30
      )
    )
  }

  shinyApp(ui, server)
}
}
